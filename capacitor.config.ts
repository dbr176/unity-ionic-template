import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sample-pwa-unity',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
